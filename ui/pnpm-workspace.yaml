# pnpm 10+ refuses to run dependency build scripts unless they are named here.
# unrs-resolver is the native module resolver behind eslint-config-next; it needs
# its install script to fetch the platform binding, or `pnpm lint` fails.
allowBuilds:
  unrs-resolver: true
