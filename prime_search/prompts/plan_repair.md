Your block did not produce a usable plan.

```
{problem}
```

Fix it and respond with one corrected fenced Python block, nothing else. This is your
last attempt — if it fails the run falls back to a generic plan.
