Your reply did not contain a usable report.

```
{problem}
```

Respond with exactly one fenced `json` block holding the report, using the keys listed
above, and nothing else. This is your last attempt: if it fails, the run falls back to a
simpler review.
