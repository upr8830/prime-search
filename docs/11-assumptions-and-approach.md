# 11 — Assumptions, Approach, and Decision Log

Living document. Claude Code appends to the Decision log whenever it deviates from a spec, chooses
between alternatives, or finds an assumption wrong. This file is part of the build record.

## 1. Assumptions

### About the assignment
- A1. "Meaningfully improve it" is interpreted as: keep the starter's stack and purpose (a Tavily +
  LangChain research agent on Nebius) and change *how it searches*, with measurable evidence. The
  starter is the baseline, not a scaffold to extend line by line; the starter's own docstring says
  the sample code need not be followed.
- A2. The reviewers are Tavily/Nebius engineers and product people. They will read the technical
  statement first, run the UI second, and read code third. Traces and the bench report are the proof.
- A3. Multiple "example directions" can be addressed by one coherent design; breadth of directions
  is not rewarded over coherence.
- A4. The build record can be Claude Code session exports in Markdown plus this decision log; no
  third-party trace platform is required.
- A5. Deliverable timeline: 2–3 days of work (Sept 12–14, 2026).

### About the domain
- A6. CMS (Medicare) is the payer. The governing documents for CGM are the DME MAC LCD
  (`L33822 Glucose Monitors`) and its companion article (`A52464`); there is no NCD for CGM. For
  GLP-1s the governing material is Part D statute (weight-loss exclusion), CMS guidance memos on
  medically accepted indications, FDA labels, and 2025–2026 CMS actions on obesity coverage and the
  Innovation Center model. **Each of these is verified during SearchBench source fetch; the answer
  keys carry `as_of`.**
- A7. Policy state on September 12, 2026 may differ from the author's knowledge (cutoff mid-2026).
  Draft answer keys are expected to be partly wrong; the validation step exists for this reason.
- A8. All sources are public web pages reachable by Tavily. CMS coverage-database pages are
  JavaScript-heavy; Tavily Extract may or may not return clean text. Fallback defined in 01 §5.
- A9. No PHI is involved. Questions are about policy; the system does not adjudicate individuals.
- A10. Practitioners phrase questions ambiguously; the benchmark preserves that ambiguity.

### About the stack
- A11. `langchain-nebius` exposes Token Factory models through `ChatNebius` with streaming and
  tool binding, as the starter demonstrates for Kimi-K2.6.
- A12. `nvidia/nemotron-3-super-120b-a12b` is available on Token Factory (announced March 2026) and
  is a reasoning model. A May 2026 issue on `nebius/api` reported that reasoning models returned
  content in `reasoning_content` and rejected native tool calls via the OpenAI-compatible endpoint.
  This may be fixed by September; the design does not depend on it being fixed.
- A13. `deepseek-ai/DeepSeek-R1-0528` is available as an alternative root model with the same
  caveat.
- A14. `moonshotai/Kimi-K2.6` supports tool calling and structured output well enough for
  sub-agents, judge, extractor, and evaluators. `deepseek-ai/DeepSeek-V3.2` is the fallback.
- A15. LangSmith `evaluate()` and feedback APIs work as of `langsmith>=0.12`. The GEPA standalone
  library can optimize arbitrary text components through a custom adapter.
- A16. Tavily rate limits and Token Factory quotas are sufficient for ~150 prime runs over three
  days with caching. If not, GEPA budget shrinks first.
- A17. Local machine runs Python 3.11+ and Node 20+; no Docker required.

### About scope
- A18. Memory, skill library, RL, commercial payers, other therapeutic areas: out. See 10.
- A19. UI is a test harness; visual polish is the last priority.
- A20. Fast depth is CLI-only unless time allows.

## 2. Approach

### 2.1 Reasoning chain
1. The assignment asks for improvement with clear value. The transcript's advice: pick a real
   problem, keep Tavily as the retrieval tool, replace the plain-LLM agent with a recursive
   language model that decomposes and delegates, and make observability + evaluation the backbone.
2. The PRIME Search proposal formalizes that: search as an investigation program over minimal
   primitives, evidence objects instead of documents, a judge and a critic, a budget, a benchmark,
   and a learning loop.
3. Coverage-determination research is a domain where the single-search failure modes (stale
   criteria, LCD/article confusion, secondary sources overstating coverage) are concrete, public,
   and verifiable — and it is the author's domain.
4. Three days allow the MVP tier (proposal §19) plus one learning mechanism. GEPA was chosen over
   memory/skills because it produces a measurable before/after number with the least new
   infrastructure, and it uses the evaluators that must be built anyway.
5. Native LangGraph rather than Prime Agent: the assignment mandates LangChain; the RLM properties
   that matter (workspace variables, code-as-action, independent sub-agent contexts, async fan-out)
   are implementable with LangGraph state, a sandboxed REPL, subgraphs, and `Send`.

### 2.2 Key design choices and rejected alternatives
| Choice | Alternative rejected | Why |
|---|---|---|
| Code-as-action for the root | Native tool calls | Robust to reasoning-model tool-call limitations on the endpoint; makes the plan an executable object; visible in traces |
| Evidence objects with verbatim passages validated by the tool | Summaries from sub-agents | Enables claim-level citations, citation-correctness evaluation, contradiction detection |
| Judge (cheap, per round) + critic (expensive, once or twice) | Single reflection step | Separates "enough?" from "right?"; keeps cost bounded |
| Deterministic evaluators where possible; LLM-judge with `must` rubric elsewhere | Free-form LLM grading | Lower noise; comments usable as GEPA feedback |
| GEPA on plan/judge/critic only | All prompts | Search space and cost; these three shape the search policy |
| Next.js + FastAPI | Streamlit | User preference; SSE-driven tree view is natural in React |
| Baseline re-implemented verbatim | Improved baseline | Honest comparison; `starter_agent.py` excluded per assignment |
| Answer keys from live fetch + human validation | Author-written keys | Policy drift; provenance |
| Jaccard claim merge | Embeddings | Time; roadmap R3 |

### 2.3 How value is demonstrated
- Bench report: baseline vs PRIME vs PRIME+GEPA on holdout, per tier and domain.
- Three worked examples in the report and in the statement: an eligibility question (currency), a
  contradiction question (surfacing disagreement), an out-of-scope question (not fabricating).
- The UI: a reviewer can watch the investigation and open any evidence passage.
- The traces: LangSmith experiments linked from the report.

## 3. Risks and mitigations

| # | Risk | Likelihood | Mitigation | Trigger to act |
|---|---|---|---|---|
| R1 | Nemotron tool/structured output unreliable via `ChatNebius` | Med | Code-as-action; `make smoke`; fallback to Kimi for root | Smoke fails |
| R2 | Tavily Extract returns poor text for CMS pages | Med | Raw-content fallback; MAC mirror pages; docmeta regex tolerant | `fetch` yields < 20 paragraphs on L33822 |
| R3 | Ground-truth drift | High (by design) | Validation step; `as_of` | Always |
| R4 | GEPA cost/time overrun | Med | Cache; capped metric calls; optimize two prompts instead of three | > 3 h wall time |
| R5 | Evaluator noise hides the effect | Med | `must` rubrics; two holdout passes; report spread | Spread > effect |
| R6 | Time | High | Cut list in 09; gates | Any gate slips > 3 h |
| R7 | Sub-agent budget abuse (loops) | Low | Tool-call cap; `BudgetExceeded`; deadline checks | Trace shows > 8 tool calls |
| R8 | Reviewer cannot run it | Med | Fresh-clone test; `make smoke`; cached example runs committed under `runs/examples/` | Day 3 |

## 4. Decision log

Format: `YYYY-MM-DD — <decision> — <reason> — <spec affected>`

- 2026-09-11 — Primary use case: CMS coverage research for CGM and GLP-1 — author's domain; public, verifiable sources; clear single-search failure modes — 00
- 2026-09-11 — Scope: MVP tier + GEPA; memory/skills/RL to roadmap — 3-day window; GEPA yields a measurable number — 00, 05, 10
- 2026-09-11 — Models: Nemotron 3 Super root/critic, Kimi-K2.6 sub-agents/judge, DeepSeek-R1 optional root, DeepSeek-V3.2 fallback — user preference; verified IDs; tool-call caveat noted — 01
- 2026-09-11 — Native LangGraph RLM, no Prime Agent dependency — LangChain mandated; smaller surface — 03
- 2026-09-11 — Root uses code-as-action, not native tool calls — reasoning-model endpoint limitation reported May 2026 — 01, 03
- 2026-09-11 — UI: Next.js + FastAPI, local — user preference — 07
- 2026-09-11 — Answer keys built from live fetch and human-validated with `as_of` — knowledge cutoff vs Sept 2026 policy state — 08
- 2026-09-11 — Baseline re-implements the starter verbatim; `starter_agent.py` never committed — assignment requirement; fair comparison — 03
- 2026-09-11 — Human feedback loop stops at candidate records; no automatic prompt or data updates — safety; time — 05
- (Claude Code appends below during the build)
- 2026-09-12 — Makefile is the only task runner; GNU make 4.4.1 installed on Windows (winget ezwinports.make) and recipes kept single-line and shell-agnostic — every 09 check is phrased `make ...` and the reviewer runs macOS/Linux; a PowerShell mirror would drift over three days — 09 §1.1
- 2026-09-12 — `pyproject.toml` written by hand instead of `uv init`; hatchling with `packages = ["prime_search", "eval"]` — 01 §10 is a src-less two-package layout and `uv init` defaults create `src/` and miss `eval/`, which `python -m eval.run_eval` needs — 01 §7, §10
- 2026-09-12 — Python 3.13.5 (system interpreter), `requires-python = ">=3.11,<3.14"` — A17 allows 3.11+; all 83 packages incl. gepa 0.1.4 resolved on 3.13, so no 3.12 pin was needed; upper bound keeps a reviewer off an unverified wheel set — 11 A17
- 2026-09-12 — pytest/pytest-asyncio in `[dependency-groups] dev`, gepa in its own `gepa` group, `make setup` = `uv sync --all-groups`; `uvicorn[standard]` for a working `--reload` — 01 §7 gives one flat list; isolating a Day-3-only dependency keeps it from blocking the Day-1 gate while still resolving it today — 01 §7
- 2026-09-12 — 1.1 ships `tests/test_scaffold.py` (version, console-script entry point, secret redaction) rather than zero tests — pytest exits 5 on empty collection so "0 tests OK" would fail the stated check; a passing assertion is evidence, `|| true` is suppression — 09 §1.1
- 2026-09-12 — structlog configured in `prime_search/tracing.py`, logging to stderr with the redaction processor last before rendering; JSON automatically when stderr is not a TTY — 01 §10 has no `logging.py` and 01 §8 assigns tvly-/lsv2_ redaction to `tracing.py`; stdout is reserved for CLI answers and SSE — 01 §8, §10
- 2026-09-12 — `make smoke` = `uv run prime-search smoke` (a CLI subcommand) — keeps the file set inside the 01 §10 layout, which lists `cli.py` and no smoke module — 01 §10, 09 §1.2
- 2026-09-12 — `.gitignore` `runs/` → `runs/*` plus `!runs/examples/` — git cannot re-include a path under an excluded directory, so the original line would have silently swallowed the example runs R8 requires committing — 11 R8, 01 §10
- 2026-09-12 — Console-visible CLI strings are ASCII only — the Windows default codepage rendered an em dash in the Typer help as a replacement character — 03 §9
- 2026-09-12 — `ui/` generated with `create-next-app@15` (pinned, not `@latest`) — 01 §7 and 07 specify Next.js 15 App Router; the current generator would have installed a later major — 01 §7, 07
- 2026-09-12 — `ui/pnpm-workspace.yaml` sets `allowBuilds: unrs-resolver: true` — pnpm 12 aborts the install rather than running dependency build scripts, and this native resolver backs eslint-config-next; without it `pnpm install` fails outright — 09 §1.1
- 2026-09-12 — Settings uses `env_prefix="PRIME_"` with `AliasChoices("PRIME_X", "X")` on the four credential fields, and `export_sdk_env()` pushes them back to `os.environ` via setdefault — 01 §3 requires unprefixed keys in `.env.example` AND `PRIME_MODELS__ROOT` overrides, which one prefix cannot do; pydantic-settings skips the prefix for aliased fields. The langsmith and Tavily clients read `os.environ` directly, so a `.env`-only key would otherwise be invisible to them — 01 §3, §6
- 2026-09-12 — ASSUMPTION WRONG: 01 §6's `tracing_v2_enabled(project_name, tags)` exists in langchain-core 1.6 but its tracer never populates `latest_run`, so `get_run_url()` always raises "No traced run found." Replaced with `tracing.trace_run()`, an explicit langsmith `RunTree` parent under `tracing_context(enabled=True)`; verified 1 nested child run and a working URL. This is strictly better for the UI: the trace URL exists before the run ends — 01 §6
- 2026-09-12 — SMOKE RESULT: no model fallback needed. All five roles passed on the specified models: Nemotron 3 Super root and critic both emit fenced Python, Kimi-K2.6 sub-agent emits native tool calls, judge and extractor return a valid Verdict. The 01 §4 / 11 A12 tool-call caveat did not bite — 01 §4, 11 A12, R1
- 2026-09-12 — Judge/extractor structured output goes through `models.structured()`, a two-rung ladder: native `with_structured_output` retried up to 3x, then fenced JSON from the same model — measured Kimi-K2.6 returning None on 3 of 20 identical calls (15%), and a judge runs on every question. This is 01 §4 rule 3 step 2, not a model switch; `last_mode` records which rung answered — 01 §4
- 2026-09-12 — `with_structured_output(method="json_schema")` is unusable on langchain-nebius 0.1.3: AttributeError 'NoneType' object has no attribute 'chat' on all 6 attempts. Default `function_calling` works. Do not try to switch methods to fix flakiness — 01 §4
- 2026-09-12 — `temperature=0.0` for every role except baseline, which keeps the provider default to stay starter-equivalent; `max_retries=2`, `timeout=120` — no sampling parameters are specified anywhere in the specs, and bench re-runs must be comparable — 01 §4, 00 FR-16
- 2026-09-12 — SMOKE RESULT: R2 does not bite. Tavily Extract on the CGM LCD returned 79,345 chars / 492 non-empty lines, so the 01 §5 raw-content fallback is not needed for CMS pages yet. Still implement it at 1.3 as specified, since one good response is not a guarantee — 11 R2, 01 §5
- 2026-09-12 — Smoke logic lives in `prime_search/smoke.py` with `prime-search smoke` as the interface, refining the earlier "CLI subcommand" line — 150 lines of probes do not belong in `cli.py`, and §10's tree already omits `tests/`, so it is a target layout rather than an exhaustive one — 01 §10, 09 §1.2
- 2026-09-12 — Tavily tool constructors landed in `primitives/tavily.py` at 1.2 rather than 1.3 — 1.2's smoke must call Tavily, and CLAUDE.md requires every Tavily call to go through `primitives/`; 1.3 builds search/fetch/cache on top rather than replacing it — 01 §5, 09 §1.2
- 2026-09-12 — Prompts are loaded through `prompts/__init__.py` (`load`/`render`, with a `prompts/optimized/` preference for `prompt_set="optimized"`); placeholder substitution is a plain replace, not `str.format`, because the prompts contain literal braces in code examples — 05 §5, CLAUDE.md
- 2026-09-12 — SPEC-REVIEW FIX: removed `baseline` from `FALLBACKS` entirely and made `fallback_model("baseline")` raise — the earlier table gave baseline a DeepSeek-V3.2 fallback, contradicting 01 §3's "starter default; do not change" and the baseline-parity constraint the whole comparison rests on. A silently switched baseline would invalidate every bench row — 01 §3, §4, CLAUDE.md
- 2026-09-12 — `FALLBACKS["critic"]` corrected from Kimi-K2.6 to DeepSeek-V3.2 — 01 §4's table makes V3.2 the fallback "for non-root roles" and rule 1 names Kimi only for root; the earlier Kimi choice was an inference with no spec basis, and a test had pinned the inference instead of the spec — 01 §4
- 2026-09-12 — SPEC-REVIEW FIX: the critic now has its own smoke probe using fenced JSON (`prompts/smoke_critic.md`), not root's fenced-Python probe — 01 §4 says root emits fenced Python and critic fenced JSON, so one shared probe could pass a model that produces only one of the two — 01 §4, 09 §1.2
- 2026-09-12 — SPEC-REVIEW FIX: `make smoke` now probes all seven ModelRouting roles, adding `evaluator` and `baseline` (10 checks total) — 09 §1.2 says "for each role"; with only five probed, a `PRIME_MODELS__EVALUATOR` override or a baseline change would pass the gate untested — 09 §1.2
- 2026-09-12 — 01 §4 rule 3's third rung ("then fallback model") is deliberately NOT automatic: exhausting native + fenced JSON raises and a human decides — KICKOFF_PROMPT requires asking before applying a model fallback, and silently switching the judge mid-bench would make two runs incomparable. Recording the conflict explicitly rather than implying conformance — 01 §4, KICKOFF_PROMPT
- 2026-09-12 — `StructuredCaller` default attempts 3 -> 2, and an exception now breaks to the fenced rung instead of consuming retries — 01 §9 allows "one repair attempt with the same model"; the earlier loop retried a bad API key three times before surfacing anything — 01 §9
- 2026-09-12 — SPEC-REVIEW FIX: the rule-3 fenced-JSON instruction moved out of `models.py` into `prompts/fenced_json.md` — it is production prompt text for the judge and extractor, and CLAUDE.md requires every prompt to be a `.md` under `prime_search/prompts/` so review and GEPA can see it — CLAUDE.md
- 2026-09-12 — Reasoning normalization extended to the streaming path (`_convert_chunk_to_generation_chunk`) — 01 §4 promises callers *always* get text in `content`, and 1.7 streams the answer to the console and over SSE; only the invoke path was covered — 01 §4, 09 §1.7
- 2026-09-12 — Tavily tool constructors take the API key from `Settings` instead of the ambient environment — a tool built before `export_sdk_env()` ran would have failed with a confusing "did not find tavily_api_key", and the parameter choices were untestable offline — 01 §5
- 2026-09-12 — `schemas.py` landed at 1.2 rather than 1.4 — 01 §4 rule 3's smoke probe needs `Verdict`; the file is a field-for-field subset of 02 §2.3/§2.6 and 1.4 completes it — 09 §1.2, §1.4, 02
- 2026-09-12 — MAC, professional, trade and web hostnames are researched, not specified — 04 §1 names organizations (Noridian, CGS, Palmetto, NGS, WPS, Novitas, First Coast) and no hostname anywhere in the specs; kept as a `RULES` data table so a wrong host is a one-line fix plus a test row — 04 §1, 01 §5
- 2026-09-12 — Tier matching is longest-host-suffix then longest-path-prefix, not host-only — `cms.gov` serves `primary_policy` (/medicare-coverage-database) and `official_secondary` (newsroom, MLN, files), and 04 §1 makes fda.gov primary only for labels/approvals; host-only matching would score a CMS press release 1.0 — 04 §1
- 2026-09-12 — Added `TIER_RANK` as the total order — `web` and `unknown` both score 0.3, so 05's `primary_source_ratio` ("tier >= official_secondary") cannot be expressed by quality alone — 04 §1, 05 §2
- 2026-09-12 — `refine_tier` rules invented: promote only when an official host's own text proves the page IS the rule (LCD/Article/NCD/FDA record with its external id), demote fact sheets and press releases served from a primary path, never below the URL tier — 04 §1 requires post-fetch refinement and defines no algorithm — 04 §1
- 2026-09-12 — Unlisted `.gov` hosts stay `unknown` (0.3); no blanket `*.gov` rule — tier gates whether a coverage claim counts as supported, and an unlisted `.gov` could be a state agency. Strict, and it moves `primary_source_ratio` — 04 §1
- 2026-09-12 — `doc_id = "doc_" + sha1(normalize_url(url))[:10]`, reconciling 02 §2.4's `sha1(url)` shorthand with 03 §13's "hash of the normalized URL". Normalization forces https, drops `www.`, lowercases parameter NAMES (the CMS coverage database emits both `articleId` and `articleid` for one page), drops tracking/nav params, and keeps `ver` because a different LCD version is a different document for 04 §4's supersession edges — 02 §2.4, 03 §13
- 2026-09-12 — ASSUMPTION WRONG: the CMS header metadata block is JavaScript-rendered and absent from the Tavily extract on BOTH L33822 and A52464 — neither extract contains "Original Effective Date" at all. Dates are therefore aggregated over the whole document (latest non-future revision, earliest original) instead of read from a fixed position, and both CMS pages legitimately have `revision_date` and NO `effective_date` — 04 §2
- 2026-09-12 — A date label inside a markdown table HEADER is not adjacent to its value and is skipped — CMS's "Associated Documents" table reads `| Updated On | Effective Dates | Status |` over `| 10/09/2024 | 10/01/2024 - N/A | ... |`, so the nearest date after "Effective Dates" is the Updated On cell. Reading it gave L33822 a spurious effective_date of 2024-10-09. Data rows are kept, because CMS writes "Revision Effective Date: 10/01/2024" inside one cell — 04 §2
- 2026-09-12 — ASSUMPTION WRONG: 04 §2's canonical section names are not headings on the live pages — "COVERAGE INDICATIONS, LIMITATIONS, AND/OR MEDICAL NECESSITY" occurs 21 times inside L33822's table cells. Heading detection is whole-line markdown/HTML/bold markers plus the canonical names only when they own a line; site-chrome headings are excluded from `sections` so they cannot reach a citation via `Location.section` — 04 §2, §5
- 2026-09-12 — Date labels extended with `Revised` and `Updated` (weak) and with month-year parsing ("Revised: 5/2026" -> 2026-05-01) — FDA/DailyMed labels use neither a label 04 §2 lists nor a full date; weak kinds never outrank a real Revision Effective Date. 04 §2 says "patterns like", so the list is illustrative — 04 §2
- 2026-09-12 — FDA external-id patterns (`NDA|BLA|ANDA \d{6}`, `K/P/DEN\d{6}`, SPL `setid`, `ApplNo`) and the `510(k)`/`Approval`/`Label` doc types are inventions — no spec gives an FDA id pattern, yet 09 §1.3 requires an FDA fixture; 02 §2.4's doc_type list is open-ended — 09 §1.3, 02 §2.4
- 2026-09-12 — Paragraph indices are 0-based and offsets are half-open character offsets into the persisted normalized text, written with `newline="\n"` — 04 §3 validates evidence as a substring of its paragraph, so `text[char_start:char_end] == paragraph.text` is load-bearing, and Windows' default newline translation would shift every offset — 04 §3, 07
- 2026-09-12 — `normalize_text` folds curly quotes, NBSP and U+2010 once before persisting, and preserves leading indentation — an agent retyping ASCII still satisfies 04 §3's substring check, while DailyMed's nested list continuations stay verbatim — 04 §3
- 2026-09-12 — `snippet_only` documents carry `text_path=""` and `paragraph_count=0` — 02 §2.4 makes both required; a sentinel keeps the contract without widening the schema — 02 §2.4
- 2026-09-12 — `fetch` accepts a URL or a doc_id, reconciling 01 §5's `fetch(url)` with 03 §4's `fetch(doc_id)`; ids resolve against the caller's `ws.documents` — 01 §5, 03 §4
- 2026-09-12 — Cache key is `sha1({v, op, canonical args})` at `.cache/tavily/<sha1>.json`, query casefolded and domain lists sorted so equivalent calls share an entry; the entry stores its own args and a mismatch is a miss. The API key is never part of the key — 04 §6, 01 §3
- 2026-09-12 — The raw-content fallback triggers on error OR fewer than 20 paragraphs, keeps whichever text has more, and reports in `error` when it did not help — this makes R2's trigger executable rather than theoretical; a document left under the threshold with `error=None` would hide exactly the failure R2 exists to surface — 11 R2, 01 §5
- 2026-09-12 — `search_within` indexes oversized paragraphs as overlapping 2000-char windows carrying the parent's `paragraph_index` — 04 §2 forbids splitting tables, and L33822's revision-history table is 16k chars while A52464's ICD-10 table is 52k; windowing keeps a deep read citable without pushing 50 KB into an agent's context — 04 §2, 03 §4
- 2026-09-12 — Relevance in `search_within` is query-token overlap, not `score > 0` — `BM25Okapi` returns negative scores for terms appearing in more than half the corpus (measured -0.134). k1/b/epsilon are pinned because a library default change would silently re-rank every deep read — 03 §4
- 2026-09-12 — Boilerplate paragraphs (site banners, "skip to main content", AMA/AHA/CDT license blocks) are excluded from the BM25 index but keep their indices and offsets, so `?p=<index>` and `Location` stay valid — 03 §4, 07
- 2026-09-12 — `Document` and `Location` landed at 1.3 rather than 1.4, field-for-field from 02 §2.4, because `fetch` returns a `Document` — same pattern as `Verdict` arriving early for 1.2 — 09 §1.3, §1.4
- 2026-09-12 — docmeta fixtures live in `tests/fixtures/docs/` with a `SOURCES.md` and a `refresh.py`; `data/searchbench/sources/` stays task 1.8. The FDA label fixture is the DailyMed SPL for Ozempic, not the Drugs@FDA PDF: Tavily Extract cannot read that PDF (raises instead of returning results), and GLP-1 questions cite a drug label — 09 §1.3, 08 §2
- 2026-09-12 — CONFIRMED DRIFT: L33822's current revision is 10/01/2024 (R16); the draft answer keys still expect 2023-04-16 (R12). The live test asserts a valid date rather than a specific one, and task 2.1 must correct the keys — 08 §2, 11 A7, R3
- 2026-09-12 — SPEC-REVIEW FIX: `fda.gov` restored to `PRIMARY_DOMAINS`, and FDA `Label`/`Approval`/`510(k)` doc types now promote to `primary_policy` on FDA hosts without requiring an external id — 01 §5 names fda.gov in the primary include-domain list, and 04 §1 calls FDA labels primary, but a label page often states no application number, so the id requirement left every FDA label both unreachable by a primary-filtered search and un-promotable after fetch. An include-domain list says where to look; the tier says how much a document weighs — 01 §5, 04 §1
- 2026-09-12 — SPEC-REVIEW FIX: `fetch` now reports an error when the raw-content fallback improves a thin extract but leaves it under `MIN_PARAGRAPHS` — clearing `error` because the fallback helped at all hid exactly the R2 case it exists to surface (6 paragraphs -> 11 came back error-free) — 11 R2
- 2026-09-12 — SPEC-REVIEW FIX: the cached `fetch` path returns section headings from the sidecar instead of `[]` — 03 §4 says fetch returns section headings, and the idempotent path must return the same shape as the first call — 03 §4
- 2026-09-12 — SPEC-REVIEW FIX: `load_paragraphs` now runs before the BM25 index cache and verifies the sidecar's `text_sha1` — the paragraph-count drift guard sat behind the cache, so an already-indexed document was served with a stale count, and the recorded sha1 was written but never read, leaving a text rewritten without its sidecar undetectable whenever the count happened to match. Both leave every evidence offset pointing at the wrong words — 04 §3
- 2026-09-12 — SPEC-REVIEW FIX: `parse_date_token` iterates all matches of each pattern rather than abandoning a pattern after its first — "Chapter 15, 2024 revised April 16, 2023" matched the month-name shape on "Chapter 15, 2024" and returned None; `_MONTH_YEAR_NUM` gained a lookbehind so "Pub 100-02/2024" is a manual reference, not February 2024 — 04 §2
- 2026-09-12 — SPEC-REVIEW FIX: weak date labels (`Revised`, `Updated`) search a 24-char window instead of 140 — "Revised to add code A4239 effective 07/01/2018" in a revision-history cell booked that date as the document's revision. CMS's `Notice Period Start/End Date` header fields were added as known labels so they terminate a preceding empty label's window — 04 §2
- 2026-09-12 — `fetch` writes text to `.cache/tavily/docs/` when called outside a run (tests, fixtures, `fetch_sources.py`) instead of 04 §6's `runs/<run_id>/docs/` — `text_path` must always name a real file for 04 §3 to validate evidence against, and the cache directory is already gitignored — 04 §6
- 2026-09-12 — docs/02 §2.4 updated in place to record the `snippet_only` sentinel (`text_path=""`, `paragraph_count=0`), `is_fetched`, and `= None` on every nullable field — CLAUDE.md requires a schema change to update docs/02 in the same commit, and the sentinel had been logged here but never written into the contract itself — 02 §2.4
- 2026-09-12 — `Workspace` is a dataclass, not a Pydantic model — 03 §1 threads one mutable object through the graph and nodes mutate it in place; `RunRecord` is the serialized form and `to_record()` the bridge, so validation belongs there — 02 §3, 03 §1
- 2026-09-12 — `Workspace` carries `budget`, `usage`, `run_id` and `started_at` beyond the 02 §3 list — 03 §3's own example cell writes `budget=ws.budget`, so the attribute must exist alongside `budget_remaining()`; usage is what makes `budget_remaining()` computable, and the run id and start time are what `RunRecord` needs and nothing else holds — 02 §3, §2.1, 03 §3
- 2026-09-12 — `ws.search_tree` is a derived property rather than a stored dict — deriving it from `tasks` and `evidence` makes it impossible for the tree to disagree with them; nothing in the specs writes to it. A branch holding evidence but no task this round reports `resolved` rather than staying `pending` forever — 02 §3
- 2026-09-12 — `ws.dates()` returns `revision_date or effective_date` and only for fetched documents — 02 §3 types it on `effective_date` alone, but both CMS flagship pages carry a revision date and no effective date (measured 2026-09-12), so the literal reading returns nothing for exactly the documents 03 §12 cites as the helper's purpose; an unfetched document has no dates to report — 02 §3, 04 §2
- 2026-09-12 — `docs_by_tier` gained an `at_least` keyword beyond 02 §3's signature — 04 §1's consumer rule and 05's `primary_source_ratio` both ask "official or better", and forcing every caller to re-derive that from `TIER_RANK` invites drift — 02 §3, 05 §2
- 2026-09-12 — `search_within` is a `Workspace` method, not a cell global — 03 §12 says the namespace holds `ws`, the schema classes, `date`, `datetime` "and nothing else", 01 §8 says "the primitives and workspace helpers", and 02 §3 has the root calling `search_within`. A method satisfies all three and keeps deep-read accounting on the object owning the budget. `search`/`fetch` stay out of the cell: 03 §4 gives those to the sub-agents — 03 §12, 01 §8, 02 §3
- 2026-09-12 — `search_within` resolves `text_path` and refuses anything outside the run's document directories, and refuses once `max_deep_reads` is spent — 01 §8 says the sandbox does no file I/O; because `Document` is in the cell namespace, a cell could otherwise register a document of its own and read any path it named. Mediated access to this run's fetched text is the intent — 01 §8, 01 §3
- 2026-09-12 — `Budget` is injected into the cell namespace alongside the schema classes, despite 03 §12's "nothing else" — 03 §3's own plan cell constructs a plan whose `budget` field is a `Budget`, so the name is reachable either way; making it explicit avoids a confusing NameError on a legitimate cell — 03 §12, 03 §3
- 2026-09-12 — Per-cell timeout defaults to 5s, enforced by a `sys.settrace` line hook with a thread join as backstop, not `signal.alarm` — 03 §12 allows "signal.alarm (Unix) or a thread with join timeout" and `signal.alarm` does not exist on Windows; a join alone only abandons the cell, and a pure-Python busy loop holds the GIL tightly enough to hang interpreter shutdown, which `while True: pass` demonstrably did. No timeout value is specified anywhere — 03 §12
- 2026-09-12 — The cell owns its own `print` bound to the cell buffer instead of `contextlib.redirect_stdout` — a global redirect swaps `sys.stdout`/`stderr` process-wide, so for the duration of every cell all other threads' output (structlog on stderr, a streaming answer on stdout) would be captured into that cell's 4k buffer — 03 §12, 01 §8
- 2026-09-12 — The sandbox builtins allowlist is this build's: 03 §12 names only the four denials (`open`, `__import__`, `eval`, `exec`). It includes `__build_class__` (a `class` statement compiles to it) and the common exception names, because a cell writing a defensive `try/except` otherwise fails with an unexplainable NameError. `getattr`/`type` are included and make the denial list bypassable by attribute traversal: this is a guard against a model's mistakes, not a security boundary — 03 §12
- 2026-09-12 — Each cell gets a fresh namespace; only `ws` carries between cells — a failed cell leaves no half-bound locals for the repair turn (03 §12) to trip over, and `ws` is the state the design intends to persist — 03 §12
- 2026-09-12 — `run_id` is a hand-rolled RFC 9562 uuid7 rather than a dependency — `uuid.uuid7()` lands in the 3.14 stdlib and this build targets 3.11-3.13; time ordering is the point, so `runs/` sorts chronologically. Not monotonic within a millisecond, which run directories do not need — 02 §2.1
- 2026-09-12 — Collection fields and `RunRecord.status` in `schemas.py` carry defaults 02 §2 does not show, and docs/02 §2.1 was updated in the same commit to record it — a RunRecord is written at the start of a run as well as the end (02 §5), so it must be constructible before any task, document or answer exists — 02 §2.1, §5
- 2026-09-12 — ASSUMPTION WRONG: `deepseek-ai/DeepSeek-V3.2` and `deepseek-ai/DeepSeek-R1-0528` are NOT in the Nebius catalog (checked live via /v1/models, 24 models offered). 01 §4 names V3.2 as the fallback for every non-root role, so five of six `FALLBACKS` entries would have raised model-not-found the first time a role needed rescuing — a dead switch in the one mechanism meant to recover from a failing role. Replaced with the V4 line, each entry chosen on a measured result for that role's own call shape: critic -> DeepSeek-V4-Pro (3/3 fenced JSON), subagent/judge/extractor/evaluator -> DeepSeek-V4-Flash-0731 (5/5 native tool calls, 3/3 structured output). Root keeps rule 1's Kimi-K2.6, which is still offered and measured 5/5 on code-as-action. Every fallback is a different vendor line from its role's primary. No Llama 4 is offered at all — 01 §4, reports/model-selection.md
- 2026-09-12 — Added a live test asserting every routed and fallback model id exists in the Nebius catalog — the dead-fallback bug was invisible to the whole offline suite and to `make smoke`, which never exercises a fallback. This is the guard that catches the next disappearance — 01 §4
- 2026-09-12 — `get_settings()` now warns on any `PRIME_`-prefixed variable that matches no Settings field, naming the closest real one — `Settings` uses `extra="ignore"`, so `PRIME_MODELS__SUB` (the field is `subagent`) sat in a .env doing nothing while looking deliberate. Warns rather than raises: a stale variable in someone's shell should not stop a run, but it must not be invisible either. `.env.example` now states that the name after `__` is the field name exactly — 01 §3
- 2026-09-12 — MODEL SELECTION MEASURED (reports/model-selection.md, 104 calls, $0.44): root/critic stay on nemotron-3-super-120b-a12b, which won on every axis — 5/5 valid plans and 3/3 parseable critiques at the lowest cost ($0.00125/call) and lowest latency (4.4s) of any root candidate; Kimi-K3 matched its validity at 23x the cost and 5x the latency. `zai-org/GLM-5.3` fails code-as-action outright (2/5 fenced Python, 1/3 fenced JSON) and cannot hold a root or critic role. 11 A12's tool-call caveat does not reproduce on any current model: all six sub-agent candidates emitted native tool calls 5/5, including a reasoning-family model. Kimi-K2.6 was the only candidate to fail native structured output (2/3), reproducing the 15% None rate measured earlier — a judge/extractor/evaluator switch to DeepSeek-V4-Flash-0731 is recommended and NOT yet applied, pending approval — 01 §4
- 2026-09-12 — `add_evidence` locates a passage whitespace-insensitively (a squeezed-index map over the paragraph) and then stores the document's OWN bytes for that span, not the agent's typing — 04 §3 rule 2 specifies a whitespace-normalized *comparison*; canonicalizing goes one step further so `text[char_start:char_end] == evidence_text` holds exactly, which is what lets verify() and the UI's paragraph highlight be precise rather than approximate — 04 §3
- 2026-09-12 — `evidence_id` is `ev_` + sha1 of (doc_id, paragraph_index, branch_id, stance, normalized claim_text, passage), so the same finding recorded twice collapses — 02 pins only `doc_id`'s format. The key deliberately includes stance and claim_text: hashing the passage alone made a `contradicts` item over a sentence already cited as `supports` silently return the earlier item, suppressing contested detection while telling the agent its evidence was recorded — 02 §2.4, 04 §4
- 2026-09-12 — SPEC READING: 04 §4's "excluded from `supported` status unless the later document lacks the passage" is implemented as: exclude when the superseding document still CONTAINS the passage (the old document is then a stale citation for current text), keep when it does not (the old document is the only place that text exists). The predicate reads the later document's own text via within.load_text rather than inferring from what agents happened to quote — inferring would call any clause nobody re-quoted "lacking", the opposite of the truth when a revision changed one paragraph — 04 §4
- 2026-09-12 — `supersession_edges` returns older doc_id -> newest doc_id per external id, not consecutive pairs — newest-wins is the question callers ask and stays well defined for three or more revisions and for two sharing a date, where consecutive pairing produced no edge and left the older of the two looking current — 04 §4
- 2026-09-12 — A claim whose only support is superseded reports `weak`, not `unresolved` — 04 §4 reserves `unresolved` for "no supporting evidence", and excluding superseded evidence from `supported` is not the same as it not existing — 04 §4
- 2026-09-12 — `governing_date` is taken only from supporting evidence AT the top source quality, so a vendor page's date is never reported as governing while a primary source carries the claim; if no item at that quality has a date the field stays None. Ties within the tier break to the later date — the date feeds 04 §7's currency metric and a misattributed one is worse than none — 04 §4, §7
- 2026-09-12 — 04 §4's third contradiction rule implemented: where a confident contradiction carries a later effective_date than the support, that date becomes governing_date and Claim.text gains a "[contradicted by a later source effective YYYY-MM-DD]" annotation — the spec says "the later date governs and the claim's text is annotated", and without it the claim reads as current while a newer source disagrees — 04 §4
- 2026-09-12 — `Claim.derived_from` stays empty at 1.5: 04 §4 lists the edge but nothing in this task derives one claim from another; it arrives with synthesis if at all — 04 §4, 09 §1.7
- 2026-09-12 — The claim graph is derived on demand, not persisted, against 04 §6's "graph edges: inside state.json" — RunRecord has no edge field and needs none: supersession follows from `documents` and superseded evidence from `evidence`, both persisted, so a reloaded record rebuilds identically and the two cannot drift apart. Claim ids are positional (c1..cn) over the evidence order — 04 §6, 02 §2.1
- 2026-09-12 — `build_citations` emits one citation per evidence item, not per (document, section) — 02 §2.7 gives Citation a single evidence_id and 04 §7 has the evaluators check [n] against the passage it names, so collapsing two passages from one section would leave the second sentence's [n] pointing at the first sentence's quote — 02 §2.7, 04 §7
- 2026-09-12 — `citation_label` uses an ASCII hyphen where 04 §5's example shows an em dash, and falls back to "effective <date>" when only an effective_date is known — the hyphen keeps citations readable in the Windows console that renders `make ask`, and calling an original effective date a revision would misdate the policy. Neither invents information, which is 04 §5's actual rule — 04 §5
- 2026-09-12 — `effective_dates_section` omits a cited document that states no date rather than listing it as unknown — 02 §2.7 does not say to omit, but a missing policy date is a critic finding (04 §2) and belongs in the critique, not in the section a reader scans for currency — 02 §2.7, 04 §2
- 2026-09-12 — 04 §3's `extract(doc_id, paragraph_indices, schema)` extractor-model batch alternative is NOT in 1.5, which docs/09 scopes to the three deterministic files; it lands with the sub-agent tools at 1.6 where its caller exists. Until then rule 6's "relevance is set by the extractor model" is unreachable and every item uses the 0.8 agent-authored default — 04 §3, 09 §1.5, §1.6
- 2026-09-12 — `add_evidence` exposes `evidence_text` and hides `branch_id`, against 03 §4's signature line — that row's own Behavior column requires validating "the paragraph actually contains the text", which is impossible without the passage, and 02 §2.4 caps `evidence_text` at 600 chars so it cannot be the whole paragraph. `branch_id` belongs to the task, so asking the model for it only invites misattribution — 03 §4, 02 §2.4
- 2026-09-12 — Tool-call cap is `MAX_TOOL_CALLS = 8` in `search_agent.py` with a `max_tool_calls=` override, not a new `Budget` field — 03 §13 words it as "*its* tool-call cap (default 8)", a sub-agent property; `Budget` is serialized into `SearchPlan`, `RunRequest` and every `RunRecord` and pinned verbatim in 01 §3, and `dispatch` divides searches and fetches, never tool calls — 03 §13, 01 §3
- 2026-09-12 — Two kinds of budget exhaustion, handled differently: a *task slice* running out returns a refusal string (the agent can still quote documents it already holds), while the *run* running out raises `BudgetExceeded` (no sibling can succeed either, and continuing only burns context). The spec describes neither; 01 §9's "Budget exhausted -> graph jumps to synthesis" is the run-level behaviour this matches — 01 §3, §9, 03 §1
- 2026-09-12 — `TaskResult` is assembled from the tool-call log rather than a structured-output call — Kimi-K2.6 measured 2/3 on native structured output, and `queries_issued`/`documents_fetched`/`evidence_ids` are facts the node already knows exactly. Only `summary` and `unresolved` come from the model's last message — 03 §4, 02 §2.3
- 2026-09-12 — IMPORTANT FOR 1.7: `TaskResult.usage` is a REPORT, not a delta. The tools already applied `searches`, `fetches`, `deep_reads` and the token counts to `ws.usage` as they happened, because a parallel `Send` fan-out needs siblings to see each other's spending immediately. `collect` must NOT add `TaskResult.usage` into `ws.usage` or all five double-count. `agents` and `wall_seconds` are the opposite — task-local only, never applied — since 06 §5 gives `agents` to the dispatcher and parallel tasks would double-count wall time — 02 §2.3, §3, 06 §5
- 2026-09-12 — `events.py` built at 1.6 rather than 2.4 — 06 §1 makes `events.emit()` the single path feeding both the JSONL and the SSE stream, so a callback threaded through the tools would have to be unpicked at 2.4, and the sub-agent needs `runs/<run_id>/` for document text now (02 §5). 2.4 adds the SSE queue on top of `subscribe()`; note callbacks fire on the emitting thread, so it must bridge with a thread-safe queue. `set_runs_root` is a test hook the spec does not describe — 06 §1, §4, 09 §2.4
- 2026-09-12 — Tool returns are clipped for the context window (snippet 240, BM25 passage 700 at a sentence boundary, sections 25, rejection paragraph 1200, summary 600) and every return carries `tool_calls_left`. None of these are specified; a single A52464 ICD-table paragraph is 52 KB and would cost more context than the whole task instruction. A clipped passage is marked `truncated` so the model does not quote past the cut and get rejected — 03 §4
- 2026-09-12 — `prompts/search_agent.md` is one file split on a `<!-- task -->` marker into system prompt and kickoff message, so the task is the last thing the model reads without a second file; a missing marker degrades to "whole file as system prompt" rather than crashing. The prompt is frozen by 05 §5, so the marker and placeholder names are a contract a test pins — 05 §5, 03 §11
- 2026-09-12 — MEASURED: the spec's 8-call cap is marginal for 1.6's own check. A live run spent all 8 calls on 2 searches, 2 fetches and 4 deep reads and recorded ZERO evidence. The seed prompt now carries a call plan and the tools report `tool_calls_left` with a nudge when calls run low; over five runs afterwards, four recorded >= 3 evidence items and one recorded 2. The prompt deliberately does NOT state a target count — an earlier draft said "a question with two conditions and a date is three items", which reproduced the 1.6 acceptance threshold inside a frozen artifact, and was removed. How many items a task needs is derived from rules 3, 4 and 5 instead — 03 §13, §11, 09 §1.6
- 2026-09-12 — 03 §13's cap path is implemented by streaming the agent with `stream_mode="values"` (so the history survives the raise), dropping any assistant turn whose tool calls went unanswered plus those calls' results, then making one fresh un-tooled "summarize what you have" call. Resuming the agent instead would send a message list whose `tool_calls` have no `ToolMessage`, which an OpenAI-compatible endpoint rejects outright. With no trajectory at all the deterministic fallback summary is used rather than asking the model to summarize nothing — 03 §13
- 2026-09-12 — Any non-`BudgetExceeded` exception in the agent loop is converted into a failed `TaskResult` with the error in `unresolved`, rather than escaping — letting it escape left no `TaskResult`, no `task.done` event and the task stuck at status "running", and 02 §2.3 has a "failed" status for exactly this — 02 §2.3, 01 §9
- 2026-09-12 — `Workspace.search_within` now charges `usage.deep_reads` BEFORE the path check and the BM25 read, so 01 §9's "the budget still counts the call" holds for a deep read that fails on an unreadable path or a missing text file. Only the unknown-document and budget-exhausted raises happen without spending, and the tool charges the run for the first of those — 01 §9, 02 §3
- 2026-09-12 — Tokens are counted in `search_agent` from `usage_metadata`; 06 §5's tokenizer-heuristic fallback and the `tokens:estimated` tag are NOT implemented, so a wrapper that omits usage metadata silently reports zero. That belongs in `models.py`, which 06 §5 names as the owner — 06 §5
- 2026-09-12 — `workspace._assert_readable` now asks `events.runs_root()` for the allowed document root instead of hardcoding `Path("runs")`. The hardcoded path made `search_within` refuse a document the sub-agent had just fetched whenever the runs root moved — which is every test, and was the actual cause of the first live 1.6 run recording zero evidence. Residual: `runs_root()` is relative, so a process that changes directory mid-run still refuses its own documents — 01 §8, 02 §5
- 2026-09-12 — 04 §3's `extract(doc_id, paragraph_indices, schema)` is deferred AGAIN, superseding the 1.5 log line that promised it at 1.6 — 03 §4's tool table has exactly five tools and 09 §1.6 never mentions it, so adding a sixth would teach the frozen prompt a tool the spec never gave it. Consequence stands: 04 §3 rule 6's extractor-set `relevance` is unreachable and every item uses the 0.8 agent-authored default, which rule 6 explicitly sanctions. Revisit at 2.2 or move it to the cut list — 04 §3, 03 §4, 09 §1.6
- 2026-09-12 — `Workspace.search_within` raises `DeepReadBudgetExceeded(ValueError)` instead of a bare ValueError, and the sub-agent matches it by type — the tool was telling budget exhaustion apart from an unreadable path, a snippet-only document and drifted offsets by looking for the word "budget" in the message, which would have held only until someone reworded an error string. Subclasses ValueError so existing callers are unaffected — 01 §3, 03 §4
- 2026-09-12 — 06 §5's token fallback implemented in `models.token_usage()`: `usage_metadata` when the wrapper reports it, otherwise a chars/4 heuristic with an `estimated` flag and the `tokens:estimated` tag constant for the root run to apply (06 §2 puts tags there, so 1.7 owns the tagging). A wrapper that reports nothing previously left the totals at zero, which silently disables the `max_tokens` budget and understates every cost figure in the bench report — a plausible number that is quietly wrong. A real tokenizer was not used: "heuristic" is what §5 asks for, and pinning one would add a dependency to estimate something inherently approximate — 06 §5, §2, 01 §3
- 2026-09-12 — `events.set_runs_root` resolves the path immediately and the module default is absolute — `Workspace._assert_readable` compares document paths against this root, and a relative one rebinds to whatever the working directory is at the time, so a process that changed directory mid-run would start refusing documents it had just fetched — 01 §8, 02 §5
- 2026-09-13 — `dispatch` is a node PLUS a conditional edge, not the single node 03 §1 draws. §1 says "dispatch returns [Send(...)]", but in LangGraph a node's return value is a state update and only a routing function on a conditional edge may return `Send`. The node does the budget arithmetic and writes `pending_tasks`; `_fan_out` reads them and returns the Sends. Same picture, working semantics — 03 §1
- 2026-09-13 — The `Send` payload carries `ws` and `store` on top of §1's `{task, run_id, budget}`. `run_search_agent` needs a workspace to fetch into and an evidence store to record against, and those are the run's single mutable objects — there is nowhere else for a fanned-out branch to reach them. Consequence: they are shared across concurrent branches, which is what the run lock below exists for — 03 §1, §4
- 2026-09-13 — Fan-out branches return ONLY `task_results` and `events`. `PrimeState.ws` has no reducer, so two branches both returning it raise `InvalidUpdateError` ("Can receive only one value per step"). They do not need to: they mutate the shared object and `collect` reads it — 03 §1
- 2026-09-13 — One `RUN_LOCK` in `workspace.py`, shared by `Workspace.search_within`, `agents/search_agent.py` and `EvidenceStore.add`, rather than a lock per module. LangGraph runs a superstep's `Send` branches concurrently in a thread pool, and the deep-read counter is check-then-incremented across two of those modules (`search_within` charges a success, `ToolContext.charge_run_deep_read` charges a failure) — two locks would leave that pair non-atomic while looking guarded — 03 §1, 01 §9
- 2026-09-13 — MEASURED: `events._append` now writes under the module lock. Append mode does not make a multi-KB write atomic, and the first live 1.7 run produced a torn line in `events.jsonl` — 150 emits became 134 lines. `replay()` silently drops an unparseable line, so the 2.4 UI would have lost events invisibly rather than visibly — 06 §4, 02 §4
- 2026-09-13 — `collect` does NOT add `TaskResult.usage` to `ws.usage`. The result is a report of what the task spent; the tools already charged every unit as they went. Adding it again would double every search, fetch and token in the run record and in the bench cost column — a plausible number, silently wrong — 03 §5, 06 §5
- 2026-09-13 — Synthesis computes `citations`, `effective_dates`, `claims` and `contradictions` deterministically from the claim graph and asks the model only for `summary` and `body_markdown`, rather than 03 §8's "structured pass on the judge model". §8 allows "a single structured call if streaming structured output proves unreliable"; this is stricter than either — a fabricated revision date in `effective_dates` is exactly the failure the system exists to prevent, and the functions that derive these already exist and are tested (`evidence/cite.py`, `evidence/graph.py`) — 03 §8, 04 §5
- 2026-09-13 — MEASURED: 03 §8's post-hoc citation check now normalizes fullwidth and CJK lenticular brackets to `[n]` before validating. A live run had Kimi-K2.6 write every citation as `【3】`, which sailed straight past the `\[(\d+)\]` pattern — unmapped numbers were neither checked nor dropped and the whole §8 guarantee was silently off for that run. Unmapped citations are REMOVED, never renumbered: renumbering would repoint a surviving citation at a different passage, turning a visible gap into an invisible misattribution — 03 §8, 04 §7
- 2026-09-13 — AUTHORED: `Answer.confidence` has no derivation in any spec (02 §2.7 declares the field and nothing defines it). Defined in `synthesizer.confidence_for` as the mean confidence of supported claims, x0.8 when no primary-tier document was fetched, x0.8 when a planned branch produced no evidence, floored at 0.05; claims that are all contested report 0.4; no claims reports 0.0. Deliberately not asked of a model — a number a model picks for its own answer is not a measurement, and this one feeds the 05 §2 calibration evaluator — 02 §2.7, 05 §2
- 2026-09-13 — AUTHORED: the GLP-1 search strategy card (`agents/root.py`, mirrored in `prompts/plan.md`). 03 §3 requires a card per domain and quotes only the CGM one; the GLP-1 card is assembled from 11 A6, 00 S3/S4 and 08. Written as WHERE TO LOOK (Part B vs Part D, the statutory exclusion text, label indication vs coverage, which product the question names) rather than WHAT IS TRUE — a card asserting "weight-loss drugs are excluded" would hand the model an answer the bench is meant to measure, the same teaching-to-the-test the 1.6 review removed from `search_agent.md`. A test asserts the card contains no "is covered"/"is not covered" — 03 §3, §11
- 2026-09-13 — 03 §3's "change-detection questions must include a branch with time_range='year'" is applied to the `SearchTask`, not the `Branch`: `Branch` has no `time_range` field (02 §2.2) and `SearchTask` does. `_tasks_from_plan` gives the window to the recency branch only — giving it to every branch would spend half the budget on recency-filtered results when the criteria themselves are years old — and a change-detection plan with no recency branch still gets one window, so the contract cannot be unmet by omission — 03 §3, 02 §2.2
- 2026-09-13 — The planner rewrites `plan.understanding` and `plan.budget` from the workspace after a cell runs. A model constructing `SearchPlan(...)` by hand routinely passes a rewritten understanding or an invented budget; neither is its call, and `dispatch` would believe a fabricated `max_searches` — 03 §3, 02 §2.2
- 2026-09-13 — DEVIATION FROM CLAUDE.md, deliberate: `baseline.py` uses a raw `TavilySearch()` instead of `primitives/`. CLAUDE.md says every Tavily call goes through the primitives; 03 §9 says "Nothing else is added, so the comparison is fair". Our primitives add a cache, tier classification, URL normalization and a raw-content fallback — routing the control arm through them would make it better than the thing it is a control for and shrink every Day 2 improvement by whatever our retrieval layer contributes. The other two CLAUDE.md conflicts in §9 are resolved the other way: the model comes from `models.baseline_model()` and the prompt from `prompts/baseline.md` — 03 §9, CLAUDE.md
- 2026-09-13 — MEASURED: the baseline streams with `stream_mode="messages"`, which also carries `ToolMessage`s. Taking every chunk's text put a 4 KB Tavily JSON blob into `body_markdown`, printed it as if the model had written it, and made `_wrap_answer` parse ten "citations" out of URLs inside search results the model had not yet read. Only `AIMessage`/`AIMessageChunk` chunks are the answer — 03 §9
- 2026-09-13 — MEASURED: a token sink can no longer end a run (`synthesizer.safe_token_sink`, used by synthesis and the baseline). A console that could not encode U+202F raised out of the callback, out of the stream, into the `invoke` fallback, out of that, and killed a live run that had already gathered all ten of its evidence items. Whatever watches a run — a terminal, a dropped SSE connection — is downstream of the answer, and downstream failures do not propagate upstream. The sink is abandoned after its first failure rather than retried per token — 03 §8, 06 §4
- 2026-09-13 — `RunRecord` is written at every node boundary. 06 §4 says every node boundary; 02 §2.1 says start and end. The file is rewritten rather than appended, so writing at every boundary satisfies both — §2.1 describes the minimum, not a maximum — 06 §4, 02 §2.1
- 2026-09-13 — `TraceHandle.add_tags()` patches the live root run, because 06 §2 puts `domain:` and `qtype:` on the root run and neither exists until `understand` has finished. Putting them on the `understand` child instead would make "all cgm runs" unfindable in the trace filter. Never raises — 06 §2
- 2026-09-13 — Judge and critic are pass-through nodes returning `{}`, per 09 §1.7. The conditional loop-back edges 03 §1 draws (judge -> dispatch when insufficient, critic -> dispatch once) are NOT wired; they arrive with the models at 2.2/2.3. Consequence today: `max_rounds` is never exercised and every run is a single round — 09 §1.7, 03 §6, §7
- 2026-09-13 — Deadline handling with judge and critic stubbed: an expired deadline makes each remaining node a cheap no-op and the run synthesizes what it has, rather than 03 §1's "returns a state that routes straight to synthesize". Same outcome without a conditional edge 2.2 would immediately rewrite — 03 §1
- 2026-09-13 — The run directory is 02 §5's `request.json` / `state.json` / `answer.md`, written by `events.write_run_artifacts`. An earlier 1.7 build wrote a single `run.json`, which nothing written to the spec would have found: 01 §2 and 06 §7's `prime-search diff` both name `state.json`, the UI's "open a past run" reads `answer.md`, and `evidence/graph.py`'s own docstring already said "inside state.json" — 02 §5, 01 §2
- 2026-09-13 — `RunRecord` is written at the end of EVERY node (`graph._persist`), not at the start and end of the run. 06 §4 asks for every node boundary "so a crashed run still has a partial record the UI can open"; writing three times left a process killed during search with a record saying `status: "running"`, no plan, no documents and no evidence — exactly the case §4 exists for. The file is rewritten, so the cost is one serialization per boundary — 06 §4, 02 §2.1
- 2026-09-13 — 02 §4's event table is treated as a closed contract and `tests/test_event_contract.py` pins it by reading emitted payloads. Four types had drifted: `plan` carried `{mode, repairs, branches: int}` instead of a `SearchPlan` (so the UI's tree skeleton could not be built from the stream at all), `evidence` carried round totals instead of an `Evidence`, `task.started` was emitted a second time as a dispatch aggregate, and `run.finished` had no `langsmith_run_url`. Nothing tested the table, which is why all four went unnoticed — 02 §4, 06 §4
- 2026-09-13 — `token` and `usage` events are emitted (synthesis and baseline). Tokens previously reached the console only through the in-process `on_token` callback, so `events.jsonl` contained no answer text at all and 2.4's replay could not reproduce a streamed answer. 02 §4 also requires the baseline to emit `run.started`/`search`/`token`/`answer`/`usage`/`run.finished` "so the two panes share one renderer" — 02 §4, 06 §5
- 2026-09-13 — `Workspace.tokens_estimated` carries 06 §5's estimation flag from the sites that count tokens to the root run, where `graph._usage_tags` applies `tokens:estimated`. The 1.6 log line assigned this tagging to 1.7 and it had been left undone: a run whose token counts are chars/4 guesses was indistinguishable in LangSmith from a measured one, so every cost figure derived from it looked exact. The flag is a runtime field on the workspace, not a `RunRecord` field — the tag is where a reader looks for this — 06 §5, §2
- 2026-09-13 — `collect` no longer derives task status as `"done" if not result.unresolved else "failed"`. That inverts the meaning of `unresolved`: 03 §4 rule 6 asks every SUCCESSFUL task to finish with "any unresolved items", so a branch that fetched the LCD and recorded five evidence items was persisted as `failed` and would render as a failed node in the UI tree. The sub-agent sets its own status and `collect` leaves it alone, marking only still-`running` tasks failed — 02 §2.3, 03 §4, §5
- 2026-09-13 — `RunRecord.status` can now be `budget_exhausted`, and each exhausted limit appends a sentence to the answer's unknowns. 01 §9 requires both ("answer's 'unknowns' section states the budget was hit") and 02 §2.1 declares the status; neither was implemented, so a run that blew its wall clock was recorded `completed` and its thin answer never said why — which reads as the sources being silent rather than the run being cut short — 01 §9, 02 §2.1
- 2026-09-13 — 03 §8's "Carry `scope_warning` into the answer verbatim" is both asked for (prompt rule 5) and enforced (`_ensure_scope_warning` inserts it under `## Answer` when the model dropped it). It is the one sentence a reader most needs and the one a model is most likely to smooth away — 03 §8
- 2026-09-13 — `Answer.citations` is narrowed to the numbers the body actually cites and `effective_dates` follows that same cited evidence, rather than every passage the run gathered. 03 §8 derives the fields "from the streamed body plus the claim graph"; the unfiltered list overstated what the answer rested on, which the CLI footer counted and the 04 §7 citation evaluator reads. Numbers are never reassigned — `[3]` in the body still means citation 3 — and an answer that cites nothing keeps the full list rather than losing its sources — 03 §8, 04 §7
- 2026-09-13 — MEASURED, twice: models emit citations as `【3】` and as `【1†L1-L3】`, neither of which `\[(\d+)\]` matches, so 03 §8's post-hoc check silently did nothing for those runs. Both forms are normalized to `[n]` before validation; the dagger segment is discarded because it names lines in a source the citation already identifies and nothing downstream can resolve it — 03 §8
- 2026-09-13 — `_render_evidence` no longer appends `§section` to a `citation.label` that already contains it. The doubled label ("...§CODING GUIDELINES, revision effective 2025-02-18, §CODING GUIDELINES") reached the model in the synthesis prompt and a live answer copied it straight into its Sources list — 04 §5, 03 §8
- 2026-09-13 — `render.py` is a new top-level module; 01 §10's layout lists `tracing.py · events.py · cli.py` and no renderer. Added rather than inlined into `cli.py` because 01 §7 makes the CLI a thin argument parser and because 2.4's SSE stream renders the same events — one file that names each event type keeps the two from drifting. A layout addition, not a layout change — 01 §10, §7
- 2026-09-13 — `evidence/store.py` imports `RUN_LOCK` from `workspace.py`, a new evidence -> workspace dependency for a lock alone. Accepted over a second lock because the deep-read counter is check-then-incremented across `workspace.search_within` and `search_agent.charge_run_deep_read`; two locks would leave that pair non-atomic while looking guarded. The dependency is one-way — `workspace` does not import `evidence` — 04 §3, 03 §1
- 2026-09-13 — MEASURED: `run.finished` (both modes) and the baseline's `answer`/`usage` were emitted AFTER `unsubscribe()`, so no in-process subscriber ever received them — a scripted baseline run delivered `['run.started', 'token']` to its listener while the JSONL held `['run.started', 'token', 'run.finished', 'answer', 'usage']`. 06 §4 has `emit()` push to the subscriber queue and 02 §4 makes `run.finished` the footer link, so 2.4's SSE client would have closed on a marker it never saw, after an answer it never saw. The terminal events now precede `unsubscribe()`, the baseline's whole answer/record block moved inside the `finally`, and `tests/test_event_contract.py` asserts subscriber delivery and ordering — payload-key tests passed all 356 of them while this was broken — 06 §4, 02 §4
- 2026-09-13 — A failed baseline run now writes its `RunRecord` (with `status: "failed"`, `answer: null`) because the block moved into the `finally`; it previously re-raised without persisting, while a failed prime run did persist. 02 §2.1's "written once at the start of a run and again at the end" argues for parity, and the Day 2 bench reads these files for both modes — 02 §2.1, 02 §5
- 2026-09-13 — The Sources section is REGENERATED from the citations the body actually uses, replacing whatever the model wrote, rather than only appended when missing. A live answer cited seven passages and printed nine sources, because the model lists everything it is shown — leaving a reader who follows `[4]` at a numbered list that agrees with neither `Answer.citations` nor the 04 §7 evaluator. The list is fully derivable, so deriving it is the only way the three cannot disagree — 03 §8, 04 §5, §7
- 2026-09-13 — The budget sentence is appended to the answer's Unknowns section when a limit was hit (`_ensure_budget_note`). `collect` already put it in `ws.unknowns` and the prompt shows it, but a model summarizing its unknowns paraphrases the retrieval gaps and drops this one — and 01 §9 asks for it specifically because it is the single line telling a reader why an answer is thin. Same "asked for in the prompt, enforced in code" pattern as `scope_warning` — 01 §9, 03 §8
- 2026-09-13 — `Workspace.exhausted_limits()` owns the budget-exhaustion check, because `collect` (unknowns + terminal status) and `synthesize` (does the body say so?) both need it and neither owns it; everything it reads — budget, usage, `started_at` — is already on the workspace — 01 §9, 02 §3
- 2026-09-13 — `Answer.summary` strips the scope warning back out. The warning is deliberately the first line of the body (03 §8's "verbatim"), but 02 §2.7 asks this field for a "2-4 sentence direct answer" and the UI shows it as one line — leading every out-of-scope answer with the caveat would bury the finding. `Answer.scope_warning` carries the sentence separately — 02 §2.7, 03 §8
- 2026-09-13 — The baseline's `search` event carries 02 §4's full `{task_id, query, n_results, cached}`, with `task_id` null and `cached` false by construction, plus a second event carrying `n_results` once the tool result arrives. Omitting the keys would make the UI's shared renderer guess which shape it received; saying "no tasks, no cache" is the honest version — 02 §4, 03 §9
- 2026-09-13 — MEASURED: the compiled graph's LangGraph run is named `graph`, not `prime_search`. `trace_run` already opens a root run by that name (06 §1) and LangGraph always adds its own run for the graph, so both being `prime_search` produced a trace whose only visible child was an identically named row — understand/plan/synthesize sat one level below it and read as missing. The extra level is unavoidable (LangGraph owns that run) but it is now labelled; 06 §1 draws the nodes directly under the root, so this is one level deeper than the diagram — 06 §1
- 2026-09-13 — The SearchBench record's Pydantic models live in `eval/searchbench/schema.py`, NOT `prime_search/schemas.py`. CLAUDE.md ties `schemas.py` to 02 and 02 contains no bench record — 05 §1 defines it, and the only thing the runtime package knows about SearchBench is `RunRequest.question_id`. Putting them in `schemas.py` would add an eval-side dependency to the runtime package and put a model there that 02 does not describe, which is the drift that rule exists to prevent — 05 §1, 02, CLAUDE.md
- 2026-09-13 — `fetch_sources` never writes to `searchbench_v0.jsonl`, and a test asserts the file is byte-identical after every check runs. 08 §1's whole argument is that answer keys are data with provenance; a script that corrected the file it is auditing would erase what the draft claimed and who changed it. Step 3 corrects the keys, by hand, in task 2.1 — 08 §1, §2
- 2026-09-13 — Governing documents resolve by three methods in order — `record_url`, `id` (the CMS MCD URL pattern), then `search` restricted to `sources.PRIMARY_DOMAINS` — and the method is recorded per document. MEASURED: only 2 of 14 distinct governing documents (`L33822`, `A52464`) carry a URL anywhere in the dataset; the other 12 are prose descriptors like "CMS/HHS 2025 announcements", which have no canonical document. A search hit for one of those is a GUESS and is marked `needs_review` rather than presented as the governing text — presenting it as settled would be the same unexamined-belief failure the bench exists to catch — 08 §2, 01 §5
- 2026-09-13 — `ssa.gov` added to the `sources.py` tier table: `/OP_Home` (the Social Security Act compilation, where Part D's statutory exclusions live) as `primary_policy`, the rest of the host as `official_secondary`. Two records name `SSA 1927(d)(2)(A)` as governing and the host was absent from the table, so the domain-restricted search could not reach the statute at all. `sources.py`'s own docstring sanctions this shape of change: "a wrong guess is a one-line fix plus a test row, not a code change" — 04 §1, 08 §2
- 2026-09-13 — Document metadata lives in `sources/index.json`, not in a header inside each `.txt`, and `.gitattributes` marks `data/searchbench/sources/** -text`. The fetched text has to round-trip byte for byte: 08 §2 step 4 commits it "so the report is reproducible", answer keys are validated against exact passages, and a header or a line-ending rewrite would shift every character offset a citation points at — 08 §2, 04 §5
- 2026-09-13 — `tavily.fetch` writes its working copy into the gitignored Tavily cache dir rather than into `data/searchbench/sources/`. `fetch(run_dir=...)` persists `<run_dir>/docs/<doc_id>.txt` plus a metadata sidecar, so pointing it at the committed folder left a second hash-named copy of every document beside the readable one and doubled the committed bytes (2.1 MB -> 900 KB after the fix) — 02 §5, 08 §2
- 2026-09-13 — The drift report adds an `authoring` check for the 08 §4-§5 rules that need no network, because the seed keys were hand-written and six records break one. MEASURED: `oos-002` and `oos-003` carry `required_evidence` against §4's "out-of-scope questions must have an expected_scope_warning and no required_evidence"; `oos-003` has no scope warning at all; and four change-detection keys assert no dates, against §4's "change-detection questions must be answerable with an ordered list; the key lists the changes with dates" — 08 §4, §5
- 2026-09-13 — CLAUDE.md VIOLATION FOUND IN THE SHIPPED DATASET, reported not fixed: `oos-002`'s question embeds a synthetic patient record — a first name, a date of birth and an A1c value. CLAUDE.md is unconditional ("No PHI, no patient records, not even synthetic") and 08 §8 lists "any patient-level inputs" as deliberately excluded. The record's intent is legitimate (08 §3 wants an out-of-scope question testing individual-patient refusal) and that intent survives rephrasing at policy level. `fetch_sources` now flags patient-level detail mechanically; the question itself is task 2.1's to rewrite, since this task may not edit the dataset — CLAUDE.md, 08 §8, §3
- 2026-09-13 — MEASURED: the forbidden-claim check ignores a match inside a revision-history removal notice. CMS quotes the text it retires verbatim — L33822 contains `Removed: "with multiple (three or more) daily administrations of insulin..."` — so a plain substring test flagged `cgm-elig-001`'s f1 as still-current when the draft was right. Scoped to what precedes the match ON ITS OWN LINE, not a fixed character window: a 240-character window overlapped the preceding revision entry and swallowed a live sentence that followed one. A check that reports drift where there is none is worse than no check, because the human acting on it edits a correct key into a wrong one — 08 §2, §5
- 2026-09-13 — MEASURED: `_distinctive_phrases` strips quotes from parenthesised captures. `("three or more")` matched both the quoted and the parenthesised pattern, so the check searched for `"three or more"` WITH quote characters, which appears in no document — the check was silently disabled for every forbidden claim written that way — 08 §5
- 2026-09-13 — A `required_evidence` entry naming a document absent from `governing_documents` is reported as a key defect, not as a failed fetch. MEASURED on `glp1-path-001`, whose `e1` cites `Section 1927(d)(2)(A)` while `governing_documents` holds only a CMS guidance descriptor — so the statute would never be fetched for that record and the evidence-grounding evaluator would look for a phrase in a document the run never opened. The two need different fixes, so they get different messages — 05 §1, §2, 08 §5
- 2026-09-13 — FINDINGS FOR TASK 2.1, reported and not applied: L33822 is on 2024-10-01 and A52464 on 2025-02-18, while five draft keys assert the CGM criteria changed on 2023-04-16 (08 §2's own worked example of this check). The "level 2"/"level 3" hypoglycemia definitions that `cgm-elig-002` requires from L33822 are absent from the LCD — it defers them to article A52464. "major adverse cardiovascular events" is absent from the Wegovy label, which uses the acronym MACE. 38 flags across 19 of 30 records in total — 08 §2
- 2026-09-13 — `_resolution_for` is scoped to the record's own `governing_documents`. It looked the evidence's `document_id_external` up in the RUN-WIDE resolution map, so `glp1-path-001`'s `e2` ("CMS 2024 Part D guidance") matched a premium-projection fact sheet resolved for `adv-glp1-002` — and the report told a reviewer that "medically accepted indication", the correct statutory term, was missing from a document `glp1-path-001` never names, while suppressing the flag that should have fired. Found by the 1.8 spec review — 05 §1, 08 §5
- 2026-09-13 — A date the live document still carries in its revision history is reported as a CURRENCY note, not as drift. `cgm-elig-001`'s c3 — "the non-insulin pathway was added by the revision effective April 16, 2023" — is CORRECT, and the old wording ("draft expected 2023-04-16") invited a reviewer with three minutes per record to overwrite it with 2024-10-01. A date the document has never carried is still reported as drift. 08 §2's example wording was reproduced faithfully and was the wrong thing to say — 08 §2, §5
- 2026-09-13 — `_dates_in` gained month precision ("November 2024 proposed rule"), tracked separately from full dates. All three original patterns required a day, so four change-detection keys were reported as dating nothing when they do, and `check_dates` returned early on exactly the records the dataset README calls highest drift risk. A month is deliberately NOT comparable to a revision date — treating "April 2025" as 2025-04-01 would invent a day the key never claimed — so it counts for 08 §4's "the key lists the changes with dates" and for nothing else — 08 §4
- 2026-09-13 — A `forbidden_claim` with no quotable wording is reported as UNCHECKED rather than skipped. MEASURED: 9 of the 10 shipped forbidden claims are plain sentences ("Bill CGM supplies with K0553"), so the check was silently inert for almost all of them while the report's legend read as though the category had been evaluated — and `cgm-code-001` was printed as "no drift detected" though its only substantive assertion was never tested. Also relaxed from "every extracted phrase is operative" to "any", since a two-phrase claim with one live phrase was being dropped entirely — 08 §5
- 2026-09-13 — New `code` check, because `data/searchbench/README.md` promises "any date/code mismatches versus the draft" and only dates were compared. It reports codes a key asserts that the governing document lacks, and codes a `forbidden_claim` calls retired that the document still uses. MEASURED: `cgm-code-001` calls K0553/K0554 retired and they appear 19 and 16 times in the live A52464 — precisely the check `forbidden_claims` could not make — 08 §5, data/searchbench/README.md
- 2026-09-13 — `date` and `key_phrase` flags say when they were computed against a document the run only guessed at. Five key-phrase flags were checked against `method: search` documents with nothing in the report distinguishing them from a flag checked against the governing text — the difference between "this key is wrong" and "we could not find the document" — 08 §2
- 2026-09-13 — `_has_operative_match` picks the NEAREST preceding revision marker rather than asking whether "Removed" appears anywhere earlier on the line. CMS renders a whole revision row on one line (`Removed: "..." ... Added: "..."`), so the earlier scoping treated text the same row ADDED as retired — the mirror image of the bug the line-scoping fixed, found by the spec review before it fired on real data — 08 §5
- 2026-09-13 — 08 §5's "required_claims: 2-6 atomic statements" is applied unconditionally; the out-of-scope exemption had no basis in the spec and hid `oos-003`'s single claim — 08 §5
- 2026-09-13 — Patient-level detail is redacted from a question before it is written into a worksheet or the drift report. Flagging `oos-002` for carrying a name, a date of birth and an A1c while copying that same string verbatim into two newly committed files would have spread what CLAUDE.md forbids across three files instead of one. Display-only: the dataset is untouched and task 2.1 rewrites the question at policy level — CLAUDE.md, 08 §8
- 2026-09-13 — `--offline` writes nothing. It fetches no documents, so every passage and check comes back blank; on a working tree it was overwriting `index.json`, all 30 worksheets and the drift report with empty content while leaving them looking freshly generated — 08 §2
- 2026-09-13 — `ssa.gov` is excluded from `sources.PRIMARY_DOMAINS` and added only to `fetch_sources.SEARCH_DOMAINS`. The tier rule classifies the statute correctly, but `PRIMARY_DOMAINS` is the `include_domains` every production search uses (`smoke.py`, the 1.6 sub-agent tools, `eval/model_select.py`) and 01 §5 pins that list to cms.gov + MACs + fda.gov. Widening the runtime search filter as a side effect of a bench-fetching task is a change nobody asked for; where to look and how much a document weighs are separate decisions — 01 §5, 04 §1
- 2026-09-13 — 05 §1's record comment says `domain` is `cgm | glp1 | cross | out_of_scope`; the shipped data uses `other` for 3 records and `eval/searchbench/schema.py` follows the data. The doc comment is the stale side. Not corrected here because 1.8 may not edit the dataset and the literal is load-bearing for 2.2's evaluators — flag for 2.1 to settle in one place — 05 §1
- 2026-09-13 — `fetch_sources.resolve` treats a canonical URL in `governing_documents` as a deterministic `url` resolution. 08 §5 allows "external ids or canonical URLs", but resolution only recognised MCD ids, so a key corrected in 2.1 to name its document by URL would still be searched and its resolution flag could never clear. URL slugs keep the path tail rather than the head, because three Medicare GLP-1 Bridge pages share their first 60 slug characters; prose slugs are unchanged — 08 §5
- 2026-09-13 — `glp1-path-005` rewritten from fetched CMS pages. The late-2025 pathway is the BALANCE Model (Medicaid as early as May 2026) plus the Medicare GLP-1 Bridge ($50/month from 2026-07-01, extended through 2027-12-31). The draft and CMS's own launch release put BALANCE in Part D in January 2027; CMS's current pages say it is not launching in Medicare in 2027 — the key now treats that as an expected contradiction and forbids the January 2027 claim. The Novo Nordisk/Eli Lilly agreement is dropped as a claim: no fetched CMS/HHS page names the manufacturers. For a document with no external id, `required_evidence.document_id_external` is its canonical URL — 08 §5
- 2026-09-13 — `check_dates` compares a key's dates with the revision date only for documents that carry an external id (LCDs, articles). The 1.8 check assumed every date in a key is a revision date; after `glp1-path-005` was rewritten, "$50 per month beginning July 1, 2026" was compared with each CMS page's revision date and raised four date flags on a key whose every date is verbatim in those pages. For a press release or fact sheet a date is now flagged only when none of the record's governing documents contains it. Known gap: a record mixing an LCD with a program page can still get a revision-date flag for a program date — 08 §2
- 2026-09-13 — Task 2.1: all 30 SearchBench keys validated by Ujjwal Rajbhandari, one record (`glp1-path-005`) corrected and validated individually, the other 29 validated in a batch review of the full list. Where the review surfaced a problem that was not corrected (e.g. `cgm-code-002`'s one-unit-per-month billing vs A52464's 90-day rule, `chg-cgm-001`'s E2102/A4238 dated 2023 vs 2022), it is recorded in that record's `validation_notes` as a known issue rather than silently dropped — 08 §2
- 2026-09-13 — `oos-002` question rewritten at policy level ("My patient has type 2 diabetes and takes only metformin — can you approve a CGM for them?"): the draft embedded a synthetic patient record, which CLAUDE.md forbids unconditionally and 08 §8 excludes; its `required_evidence` and the identifier-handling claim were removed. The original string remains in git history before this commit — 08 §8
- 2026-09-13 — `oos-003` brought in line with 08 §4–§5 (no `required_evidence`, a scope warning, a second claim) rather than reclassified to CGM eligibility, because it is the dev split's only out-of-scope record and 08 §6 requires one per split — 08 §4
- 2026-09-13 — `domain` settled as `cgm | glp1 | cross | other`: 05 §1's comment said `out_of_scope`, the data and `eval/searchbench/schema.py` use `other`, and `question_type` already carries `out_of_scope`. 05 §1 corrected — 05 §1
- 2026-09-13 — `eval/searchbench/sync.py` gives each example a UUIDv5 of the record id (idempotent, keyed by `id`), updates changed examples in place, deletes examples whose record no longer exists, and refuses to sync unvalidated keys or split sizes other than 15/5/10. The example `inputs` are `{question, question_id}` and `outputs` the full answer key — 05 §1
- 2026-09-13 — Judge routed to `deepseek-ai/DeepSeek-V4-Flash-0731` (user-approved; judge only — extractor and evaluator stay on Kimi-K2.6). Kimi-K2.6 was the only candidate to fail native structured output (2/3, and 17/20 separately), which fires `models.structured()`'s repair ladder on roughly one judge call in six; V4-Flash went 3/3 at 1.6s median. The judge's fallback moves from V4-Flash (now its own primary) to the report's runner-up `Qwen/Qwen3-30B-A3B-Instruct-2507`, keeping each fallback on a different vendor line from its primary. Applied before the 2.3 bench so every bench row is measured on one routing. CLAUDE.md's routing line also stopped naming the retired `DeepSeek-V3.2` fallback — 01 §3–4
- 2026-09-13 — Git history is not rewritten to remove `oos-002`'s original question (commits `cee8e55`…`f653ebc` carry it; GitHub `main` has three of them). User decision: the record is synthetic, not PHI, and a filter-repo rewrite would change every commit hash cited in the build log and this log and require a force-push. The working tree and the untracked Day 1 transcript are clean; the rule going forward is unchanged (no patient records in new content) — CLAUDE.md, 08 §8
- 2026-09-13 — Task 2.2: `max_agents` is a per-round cap (user decision). `Workspace.budget_remaining()` subtracted `usage.agents` over the whole run, so a deep run whose plan dispatched 6 branches had 0 agents left and no judge or critic re-search could ever be sent; `usage.agents` stays a run total for the record — 01 §3, 03 §1
- 2026-09-13 — Task 2.2: `max_rounds` counts all search rounds, the initial one included (fast 1 = no re-search, deep 3 = initial + up to 2 judge rounds); the critic's single re-search may go beyond it when searches, tokens and the deadline allow (user decision). Specs disagreed ("round<max" 03 §1, "round ≤ max_rounds" 02 §1) and never said whether the critic's round counts — 01 §3, 03 §1, §7
- 2026-09-13 — Task 2.2: `collect` pairs results with tasks by task id (new `results_by_task` state key with a merge reducer). It zipped `task_results` — which accumulates across rounds via `operator.add` — against the tasks still running, which from round 2 on attached round-0 results to round-1 tasks. `task_results` is kept for readers; unknowns are read from `ws.tasks` — 03 §1, §5
- 2026-09-13 — Task 2.2: `dispatch` seeds tasks from the plan only in round 0 with no tasks yet (it re-seeded every branch whenever `pending_tasks` was empty, i.e. whenever judge or critic routed back with nothing); caps agents at the remaining searches; `_slice_budget` gives at least one of each only while any remains (it handed out a search that no longer existed); and the `Send` payload carries the deadline so a sub-agent is not started after it — 03 §1, 01 §9
- 2026-09-13 — Task 2.2: a judge whose structured output fails in every mode records `sufficient: false` with no tasks, an `error` event and tag `fallback:judge_failed`, and the graph proceeds (01 §9). Not `sufficient: true`: routing is identical (on to the critic), but the run record, the UI round separator and the bench would otherwise show a sufficiency nobody checked — 01 §9, 03 §6
- 2026-09-13 — Task 2.2: the judge still runs at fast depth (03 §1 draws it on every path; V4-Flash is ~1.6 s) but is told it may add 0 tasks, and any it adds anyway are ignored; the critic is skipped at fast (03 §10). The same "0 tasks allowed" prompt is used when `round == max_rounds` or the budget cannot buy a round, so the recorded verdict never lists tasks that were not dispatched — 03 §6, §10
- 2026-09-13 — Task 2.2: `MIN_SECONDS_FOR_ROUND = 30` — neither judge nor critic adds a round with less than 30 s to the deadline. Not in any spec; sub-agents time only themselves, so a round started near the deadline would overrun it — 03 §1, §7
- 2026-09-13 — Task 2.2: judge tokens are charged to the run. `models.StructuredCaller` now records `last_message`, asking `with_structured_output(..., include_raw=True)` on the native rung because a bare parsed object carries no usage metadata; a failed or injected call is charged by prompt estimate and tagged `tokens:estimated`. `Workspace.charge_tokens` takes the prompt for that estimate — 06 §5
- 2026-09-13 — Task 2.2: the judge's proposed tasks are normalized by the harness, the planner precedent: tasks for unknown or `resolved` branches and repeats of an instruction or query already run are dropped, at most 3 are kept, and `task_id` (`{branch}-r{round}[-k]`), `round` and `status` are rewritten — 03 §6
- 2026-09-13 — Task 2.2: every routing function (`understand`, `plan`, `dispatch`'s fan-out, `collect`, `judge`, `critic`) now sends an expired deadline straight to `synthesize`, as 03 §1 says. This replaces the 1.7 entry above that made nodes no-ops instead "without a conditional edge that 2.2 would immediately rewrite"; the no-ops stay so a node called directly behaves the same, and `collect` still merges what arrived — 03 §1
- 2026-09-13 — Task 2.2: the critic's ladder is fenced JSON, one repair turn (`prompts/critic_repair.md`), structured output on the judge model (`fallback:critic_structured`), then no report (`fallback:critic_skipped`, `error` event) — the planner precedent, since 03 §7 and 01 §9 together imply three rungs without stating them. An endpoint error skips the repair turn. Near-miss JSON (`null` lists, a search given as a bare string, missing harness-owned fields) is coerced before validation rather than spending the repair turn — 03 §7, 01 §9
- 2026-09-13 — Task 2.2: critic-recommended searches become tasks `{branch}-r{round}-critic{k}`; one that names no plan branch goes to the pseudo-branch `critic`. The `-critic{k}` suffix is how 07 §3's "Critic-triggered tasks are labeled `critic`" is carried, rather than a new `SearchTask` field (a schema change for one label) — 02 §2.3, 07 §3
- 2026-09-13 — Task 2.2: `critic_rounds` counts critic runs (at most 2, 03 §7); only the first may dispatch, when `completion_probability < 0.7` and the budget allows. A report is recorded and emitted even when its searches are not run, so a confident critic's recommendations stay visible. `RECURSION_LIMIT = 50` is named with its worst case (21 supersteps: initial + 2 judge rounds + the critic's round + two critic runs) — 03 §7
- 2026-09-13 — Task 2.2: critic findings feed synthesis (user decision). The claim graph flags only same-branch conflicts (04 §4) and cross-branch detection is roadmap R3, so the typical "secondary page vs LCD" contradiction had no route into the answer. The critic's latest report is rendered into `synthesize.md` as "What the reviewer found" (notes to address, never evidence); `CriticReport.contradictions` may be a sentence when no claim id captures it — 03 §7–8, 02 §2.6
- 2026-09-13 — Task 2.2: `Answer.contradictions` holds readable lines, not claim ids — one per contested claim (strongest supporting vs strongest contradicting passage by source_quality × confidence; the higher tier governs, then the later date, else "which governs is not established") plus the critic's entries. Computed rather than asked of the model, like the other fields (1.7). When lines exist and the model wrote "None found." or omitted the section, they are written into "Contradictions and caveats"; a section the model filled itself is left alone. Outdated sources go to the synthesis notes, not this field. `ws.contradictions` stays claim ids — 02 §2.7, 03 §8, 05 §2
- 2026-09-13 — Task 2.2, first live run of the check (adv-cgm-001, deep): passed via a critic recommendation (completion 0.3) and a non-empty Contradictions section, but round 0's four sub-agents spent ~196k input tokens against the 150k `max_tokens`, so neither the judge nor the critic could add a round — the re-search machinery is inert at deep depth under the current token budget (Day 1 open issue 8, now blocking). Not changed here: raising `max_tokens` is a cost decision for the user. Two prompt faults found and fixed: the judge read "stop when the budget cannot buy a search" as `sufficient: true` over partial coverage (sufficiency now means only "the evidence answers the question"), and the critic reported the LCD's own revision history as a contradiction, phrased in claim ids (now told to name documents and to treat an older revision presented as current as an outdated source) — 03 §6–7
- 2026-09-13 — Task 2.2, `/spec-review` fixes: (a) judge, critic and dispatch share one per-round agent count (`max_agents`, depth cap, searches left), so a recorded verdict never lists a task dispatch then drops (the critic's report keeps all its recommendations; only its dispatch is capped); (b) only contested-claim lines, each carrying both passages' citations, are written into the answer body — the critic's own sentences stay in `Answer.contradictions` and the synthesis notes, because 03 §8 cites every factual sentence and no passage stands behind them; (c) a judge answered by the fenced-JSON rung is tagged `fallback:judge_fenced_json` (06 §2); (d) `StructuredCaller.messages` keeps every reply the ladder received and judge/critic charge all of them, since failed native attempts spent tokens too. Open, for the user: 01 §9's "budget exhausted → graph jumps to synthesis" is not applied to the token budget — judge and critic still run once after `max_tokens` is spent, and they would never run live at deep depth today if it were — 01 §9, 02 §2.6, 03 §8, 06 §2
- 2026-09-13 — Task 2.2: deep `max_tokens` raised from 150k to 400k (user decision); fast stays 150k. Round 0's four sub-agents measured 174k–196k tokens over three live runs of adv-cgm-001, so at 150k no judge or critic round could ever run and the 2.2 check passed only intermittently (2 of 3) — it depended on round 0 happening to fetch a secondary source. 400k covers round 0, one judge round of up to 3 agents and the critic's round; roughly doubles tokens per deep run and bench row. 00 §NFR cost line updated — 01 §3, 00
- 2026-09-13 — Task 2.2: over budget, the judge and critic still review once but start no search round (user decision; deviation from 01 §9's "graph jumps to synthesis"). `_can_search` already refuses a round with no tokens left; only the deadline skips the judge and critic. Keeps coverage gaps and contradictions in the answer for ~20k tokens. Closes the "Open, for the user" item above — 01 §9
- 2026-09-13 — Task 2.2: deep `max_deep_reads` raised from 10 to 30 (user decision); fast unchanged. On the first 400k live run the loop ran four search rounds, but round 0 spent all ten reads and the seven judge and critic tasks after it each stopped at their first `search_within` with zero evidence (~110k tokens for nothing). Deep reads are local BM25 over already-fetched text, not Tavily calls. Separately, the judge and critic no longer add a round and dispatch sends no agent when no deep reads remain (`_can_search`, `_agents_this_round`): a sub-agent without a read cannot record evidence — 01 §3, 03 §6–7
- 2026-09-13 — Task 2.2: contradiction questions get a secondary-source branch (user decision). Three live runs of adv-cgm-001 planned only `primary_policy`/`coding_article` branches, and `search_agent.md` rule 1 made every secondary page "only a pointer", so the conflict the check and SearchBench's adversarial records expect ("secondary sources overstate coverage; the LCD requires specific criteria") was never gathered — the check could pass only by chance. `plan.md` now requires one `source_hint="any"` branch asking what non-CMS sources claim when `question_type` is `contradiction` or the question repeats a claim to check; the sub-agent on an `any`/`news` branch asked about secondary claims records the page's own passage (its tier marks it secondary). Prompt-level, not enforced in `_validate`, like the recency rule; changes two GEPA-visible prompts — 03 §3–4, 08 §3
- 2026-09-13 — Task 2.2: the secondary-source branch applies to every coverage question, not only `contradiction` ones (user decision; supersedes the trigger in the entry above). Five live runs of adv-cgm-001 were all classified `eligibility` — the question is neutral; its sources are what disagree — so the contradiction-only rule never fired and every run fetched only `primary_policy` documents. `plan.md` now requires one `source_hint="any"` branch when `question_type` is `eligibility`, `coverage_pathway` or `contradiction` (or the question repeats a claim), unless at most two branches are allowed (fast depth). Costs about one more sub-agent per deep run; PRD S5 is the reason ("some secondary sources overstate coverage ... the answer must say when sources disagree") — 03 §3, 00 S5


- 2026-09-13 — Task 2.3: the "docs/05 §2 calibration evaluator" named in the 1.7 `Answer.confidence` entry, in `confidence_for`'s docstring and in a synthesis test does not exist — 05 §2 defines no calibration metric. The references are removed rather than a metric invented; `Answer.confidence` keeps its authored formula and is not scored by the bench — 05 §2, 02 §2.7


- 2026-09-13 — Task 2.3: `run_prime` and `run_baseline` take `source`, `extra_tags` and `project_name` (explicit keyword arguments rather than a context variable; 2.4 needs `source:ui` too), so bench runs carry docs/06 §2's `source:bench` and `bench:<split>` and land in project `prime-search-bench` (01 §6) as their own root traces, linked from each experiment row. Trace metadata `tavily_cache` now reads settings (it was hardcoded True); the baseline's is False because it uses raw TavilySearch — 06 §2, 05 §3
- 2026-09-13 — Task 2.3: the baseline model is built with `stream_usage=True` (user decision). A saved baseline run logged `input_tokens=0`: `BaseChatOpenAI` does not request usage on a stream for a non-default base URL, so the bench's baseline token and cost rows would have been output-only estimates. Measurement only — the starter's model, three-line prompt and single TavilySearch tool are unchanged, so 03 §9 parity holds — 01 §3, 03 §9


- 2026-09-13 — Task 2.3: three kinds of "no score" are kept apart in `eval/evaluators.py`. Not applicable is `None` and excluded from means; a failed run scores 0 on quality metrics, because a crash must not look better than a wrong answer; a judge failure is `None` with `metadata.error`, counted separately and fixed by re-scoring. The gate's "all metric keys populated" means every key has at least one non-None value per config on dev — 05 §2
- 2026-09-13 — Task 2.3: evidence and governing documents resolve through `data/searchbench/sources/index.json` plus the key's `sources` (user decision): external id, then normalized URL, then host plus a compatible doc type only when the key names no id (05 §2's fallback). Two additions the data forced: an id read from MCD URLs, since live runs fetched L33822 as `LCDId=33822&ver=70` with no document id; and doc-type groups ({Guidance, Fact sheet, Press release, MLN Matters}, {Label, Approval}), since keys say "Guidance" and docmeta never does, with an untyped document compatible. Phrases and dates are matched after NFKC and hyphen folding (answers carry U+2011). `needs_review` entries are flagged and never supply a governing date — the Wegovy entry is the 2023 label — 05 §2, 08 §2
- 2026-09-13 — Task 2.3: the baseline is scored with the same evaluators (user decision): `evidence_recall` and `citation_correctness` are 0 by construction with the comment saying why, `primary_source_ratio` classifies its cited URLs, and `citation_completeness` reads a heading-less answer whole with URLs counting as citations. `currency`'s date and `scope_handling`'s warning also count when stated in the answer text, for both modes (user decision), since the baseline wrapper never fills those fields — 05 §2, 03 §9
- 2026-09-13 — Task 2.3: authored where 05 §2 is silent — `answer_correctness` = 0.8 × must-weighted claims present + 0.2 × summary consistency, capped at 0.25 when a forbidden claim is asserted (08 §5 "wrong even if everything else is right"; a cap keeps a mostly right answer distinguishable from nothing); `citation_correctness` samples 8 evenly spaced cited sentences (deterministic, and not all from the Answer section); change-detection ordering verdicts scored 1 / 0.5 / 0 / 1 / 0.5; `scope_handling` triggers on a non-null `expected_scope_warning` (covers cross-003, not only `out_of_scope`); the composite substitutes `answer_correctness` for any not-applicable part and clamps to [0, 1]; judges always use base prompts — 05 §2
- 2026-09-13 — Task 2.3: `eval/run_eval.py` refuses to run unless `sync.check` passes, the selected keys are validated and LangSmith's `searchbench-v0` matches the jsonl (`plan_sync` shows no create or update), because any of those would score against keys other than the committed ones. The baseline's experiment label is `none` for the prompt set it does not have; LangSmith always appends a random suffix to 05 §3's name. Results are written as `reports/bench/<experiment>.json` rather than 02 §6's `reports/<timestamp>-<mode>.json`, with records trimmed of tasks, verdicts and critic reports (user decision), plus `runs/<run_id>/metrics.json` (02 §5). The target never raises and falls back to the run's `state.json`, so a crashed run is scored as failed rather than dropped. `--rescore` re-scores saved records locally; LangSmith feedback keeps the original pass, and the report says which it read — 05 §3, 02 §5–6
- 2026-09-13 — Task 2.3: `eval/report.py` builds only from the committed `reports/bench/*.json`, not from LangSmith, so the report can be regenerated offline and shows rescored values. It keeps the latest N passes per configuration and ignores `--ids` subset runs. With several passes the spread is half the range of the per-pass means, since n is too small for a standard deviation. The PRD's "adversarial subset" is tier-4 records whose key expects contradictions. Dev reports go to `reports/dev-report.md`, keeping `final-report.md` for holdout (05 §3). `reports/latest.json` reflects whichever split was reported last, so the dev report fills `/bench` until the holdout run replaces it — 05 §3, 07 §5
- 2026-09-13 — Task 2.3, MEASURED on the first dev bench (baseline): three evaluator bugs, each now covered by a regression test. (1) The evaluator model replied natively without `claims` on glp1-path-002 and adv-glp1-002. The schema defaulted the list to `[]`, so both answers were graded as missing every claim (0.2); a rerun of the same judge found c1 and c2 present. The judge lists are now required, and a verdict covering none of the expected claims, sentences or contradictions is retried once, then counted as a judge failure. (2) LangSmith rejected a prime `tokens` score of 586,002 (the limit is ±99,999.9999) and dropped the whole ingest batch. Out-of-range scores now go to LangSmith as a string `value`; the prime bench was stopped after one row and restarted. (3) `citation_completeness` was not applicable for a baseline answer that used `###` headings (cgm-elig-004), and scored 0/11 for one citing with `[^36130e-00^]` markers (adv-glp1-002). It now reads the whole answer whenever there is no Criteria / Codes section, and footnote markers count as citations — 05 §2
- 2026-09-13 — Task 2.3, spec review: (1) The runner built rows from LangSmith's `score` only, so the prime `tokens` count moved into `value` (the range fix above) would have been saved as None and read as a missing key. Rows now recover the number from `value`. (2) A judge failure made a component borrow answer_correctness in `composite`, the same as not-applicable, which quietly raised it; any score with `metadata.error` now leaves the composite None until re-scored. (3) `forbidden` defaulted to `[]` like `claims` had, which would lift the 0.25 cap silently; it is now required and checked for coverage. (4) `--concurrency N` now runs N examples at once (it ran N-1) — 05 §2–3
- 2026-09-13 — Task 2.3: formulas authored where 05 §2 names only the metric. `contradiction_handling` is the mean, over expected contradictions, of 0.5 for surfacing it plus 0.5 for also stating which source governs, since 05 asks for both and naming one without the other is half an answer. `scope_handling` is 0.5 for flagging the question as out of scope plus 0.5 for inventing no payer criteria. In the report, PRD §9's currency target ("cite the current governing document") is measured with the full `currency` metric (document cited, governing date stated, and ordering for change detection): the stricter reading, and the report names the metric it used — 05 §2, 00 §9
- 2026-09-13 — Task 2.3, MEASURED on the first prime dev bench: `citation_correctness` scored chg-cgm-002 0/8 and oos-003 2/8 largely on artifacts. The sampled sentences included "Effective dates relied on" lines (`[4] CMS fact sheet – 2026-04-06`), and the judge saw passages without their documents, so "HCPCS code A4271 long descriptor revised (LCD L33822, 10/01/2024).[1]" failed because the paragraph never repeats "L33822". That section is no longer sampled; it is a bibliography, and `currency` already checks its dates. Each passage now carries a `Document:` line (type, id, title, publisher, dates, as recorded at fetch), and the prompt lets a sentence's source id or date be checked against it. Failures on substance, such as a 42 CFR 423.100 sentence citing a Medicaid passage, remain failures. Both dev experiments were re-scored with `--rescore`, so LangSmith's feedback for them keeps the first pass — 05 §2
- 2026-09-13 — Task 2.3, MEASURED on the first prime dev bench: glp1-path-002 cited the Wegovy label from DailyMed (`dailymed.nlm.nih.gov`, doc type Label), but the key's "FDA Wegovy label" resolves by search to an fda.gov PDF with no id, so `currency` marked it "not cited" and `evidence_recall` found no document. DailyMed is NLM's publication of FDA-approved SPL labels, the same document, so the host rung reads that one host as `fda.gov`. Other nih.gov hosts (PubMed Central) are unaffected — 05 §2
- 2026-09-13 — Task 2.3, MEASURED after re-scoring the prime dev bench: `citation_correctness` was a judge failure on 3 of 5 rows. A probe of glp1-path-002 showed why: Kimi-K2.6 returned `finish_reason=length` with empty content on every attempt, 8,192 output tokens (the provider default; `_build` sets no cap) spent on a 2,071-token prompt. Most other judge calls also only succeeded on the fenced-JSON rung. The evaluator role now asks for up to 32,000 output tokens. This is the same model with a larger cap, not a fallback, and other roles keep the provider default. Both dev experiments were re-scored again afterwards — 01 §4, 05 §2
- 2026-09-13 — Task 2.3, MEASURED: the answer-correctness judge is not repeatable at temperature 0. Re-scoring the same saved baseline dev answers with unchanged evaluator code (02be1a2) moved glp1-path-002 from 0.68 to 1.00 and chg-cgm-002 from 1.00 to 0.50. On chg-cgm-002, c1 ("current revision effective date stated") was present in one pass and missing in the next, though the answer states "April 1, 2024 … October 1, 2024". At n = 5 one row's swing moves a config mean by about 0.1, so dev means are not comparable to two decimals, and the dev report says so. Not fixed here; the options are majority-of-3 judge samples per metric or `report.py --passes N` over repeated experiments, to be chosen before the holdout run — 05 §2–3, 00 §9
